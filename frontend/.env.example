# RotoAI Frontend - Environment Variables
# Leave VITE_API_URL empty to connect to the local FastAPI backend (http://localhost:8000).
# If running backend on a custom port/host, set it here (e.g., http://localhost:8000).
VITE_API_URL=
