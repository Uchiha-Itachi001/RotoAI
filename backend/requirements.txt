# RotoAI Backend Dependencies
# Install with: pip install -r requirements.txt

# ── Core API ──────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
websockets>=12.0
aiofiles>=23.2.1
python-dotenv>=1.0.1

# ── Image / Video ─────────────────────────────────────────
Pillow>=10.3.0
numpy>=1.26.4
opencv-python>=4.10.0.84
ffmpeg-python>=0.2.0

# ── ML (install separately — see README for GPU-specific torch command) ──
# torch>=2.3.1
# torchvision>=0.18.1

# ── Optional: Gradio debug UI ─────────────────────────────
# gradio>=4.36.0

# ── SAM 2 (install from source — see README) ─────────────
# pip install git+https://github.com/facebookresearch/sam2.git
